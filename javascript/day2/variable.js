// 변수 선언
//1) var 전역변수 : 전역객체(window, globolThis)에 프로퍼티로 추가됨
var x = 10

//2) es6버전 이후 등장

// let : 변수선언
let y;    //변수 선언
y = 20;   //변수 초기화
console.log(y);
y = 30;   //재할당
console.log(y);

// const : 상수 선언
const z = 100;  // 상수 선언과 초기화를 같이 해줘야한다.
//z = 200; // 재할당불가

